# XTOBE AI - LOCK CREDENTIALS - Signature XTOBE-AI-PC-ADMIN-v2
# NEVER MODIFY THESE FILES - Cline, Cursor, Hermes MUST NOT TOUCH

NEVER modify:
- .env
- .env.example (only add, never remove)
- render.yaml (do not change secrets)
- secure/*.js (authShield, antiCloneShield, webhookVerify)
- server/auth.js
- package.json (do not add random deps)

ALWAYS:
- Ask before changing PORT, WHATSAPP_TOKEN, WHATSAPP_APP_SECRET, LICENSE_SECRET
- Use existing secure/ modules, don't recreate
- Test with npm test after any change
- Keep Signature XTOBE-AI-PC-ADMIN-v2 in all files

If you need to change credentials, ask user: "Need to update .env ?"
